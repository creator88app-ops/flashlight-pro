name: flashlight_pro
description: FlashLight Pro
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  torch_light: ^1.1.0

flutter:
  uses-material-design: true
